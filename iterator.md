## Why

Provides a way to traverse a collection without exposing its internal structure (array, tree, graph, linked list).

> Playlist navigation (next/previous), File system traversal, Social media feed pagination.
> Most languages have this built in (Python's __iter__, Java's Iterator), so you rarely implement from scratch in interviews, but knowing the concept matters.
